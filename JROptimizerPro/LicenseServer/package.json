{
  "name": "jr-optimizer-license-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply jr-optimizer-licenses --local",
    "db:migrate:remote": "wrangler d1 migrations apply jr-optimizer-licenses --remote",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.26.0"
  }
}
